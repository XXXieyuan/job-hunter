<section class="card empty-state">
  <p class="eyebrow"><%= errorState.statusCode %> · <%= errorState.code %></p>
  <h1><%= errorState.message %></h1>
  <p class="text-muted"><%= lang === 'en' ? 'Please go back or try a different page.' : '请返回上一页，或尝试其他页面。' %></p>
</section>
